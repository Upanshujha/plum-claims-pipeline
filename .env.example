# Copy this file to .env and fill in your key. .env is gitignored.
# A free key is obtainable at https://console.groq.com — stage 10
# (rejection_explainer.py) reads it via os.environ.get("GROQ_API_KEY").
GROQ_API_KEY=
# Optional override — defaults to llama-3.1-8b-instant
# GROQ_REJECTION_MODEL=llama-3.1-8b-instant
